# Core / config
python-dotenv
groq
pydantic>=2

# Phase 2 — multimodal vector retrieval (M2L1-M2L3)
chromadb
sentence-transformers
torch
pillow
ftfy
regex
numpy
git+https://github.com/openai/CLIP.git

# Phase 3 — agents, workflow, chatbot (M3L1-M3L3)
langchain
langchain-groq
langgraph
gradio

# Phase 4 — MCP server/client/host (M4L1-M4L3)
# Pinned <2: mcp 2.x renamed FastMCP to MCPServer and changed the API. The IBM lab
# text explicitly names "FastMCP", and this repo's other proven-working MCP code
# (9-Building AI Agents with MCP/) targets 1.x - pin to match rather than adapt to
# a differently-named v2 surface.
mcp<2

# Testing
pytest
